root = "."
testdata_dir = "testdata"
tmp_dir = "tmp/scheduler"

[build]
  cmd = "go build -o ./tmp/scheduler/scheduler ./cmd/scheduler"
  bin = "./tmp/scheduler/scheduler"
  delay = 1000
  exclude_dir = ["assets", "tmp", "vendor", "testdata", "web/node_modules"]
  exclude_regex = ["_test.go"]
  include_ext = ["go", "tpl", "tmpl", "html"]
  kill_delay = "0s"
  rerun_delay = 500

[log]
  time = false

[screen]
  clear_on_rebuild = false
  keep_scroll = true

